# CONTEXT

本项目的领域语言。这里只放术语,不放实现细节、不放设计决策。

## 条目

**Character(角色)** — PGR 世界观里的可操作角色。一个角色由名字、立绘、属性、机体类型、所属组织、以及一段双语介绍构成。

**Organization(组织)** — 角色所属或独立存在的势力(如 Gray Raven、Garden)。一个组织由名字、徽记、以及一段双语介绍构成。

**Item(条目)** — Character 与 Organization 的统称。两者在界面上以同一种行式布局呈现,因此共享「名字 + 图 + 介绍」这一形态。

## 条目的构成

**Catalogue Key(图集键)** — 条目标识其立绘的名字。它是唯一的键:数据里、图集目录里、磁盘文件名上都必须是同一个字符串。任何一处不一致都会让该条目显示占位图而不是立绘。

**Imageset(图集)** — 立绘在 Xcode 资源目录中的存放单位。一个图集只服务一个 Catalogue Key。

**Bilingual Field(双语字段)** — 同一内容的英文与中文两版。介绍固定是双语;名字与其余元数据大多也是双语,但允许只发布其中一版——缺失的一版回退到另一版。

**Element Type(属性)** — 角色的攻击属性(如 Physical、Ice、Thunder、Fire)。

**Frame Type(机体类型)** — 角色的机体定位(如 Rapid、Assault、Heavy、Barrage)。

**Organization Affiliation(所属组织)** — 角色归属的组织,取值应当能在 Organization 条目中找到对应。

## 阅读状态

**Language Preference(语言偏好)** — 读者选择以哪种语言阅读条目内容。属于阅读状态,不属于条目本身;它改变同一条目的呈现,不改变条目集合。

**Onboarding(引导页)** — 首次启动时展示的五页世界观介绍。读者看完后不再自动出现,但可以主动重看。

## 数据来源

**Catalogue(图鉴数据)** — 随应用一同发布的只读条目集合,来源于 PGR 官方维基。它不是运行时可变数据,也没有远端更新。
